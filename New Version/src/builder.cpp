
#include "../include/functions.h"

using namespace std;

void boardBuilder (int * matrixVector, int rows, int collums) {
    vector <int> boats = {1, 1, 1, 1, 2, 2, 2, 3, 3, 4};

    /*Lembrar para quando for implementar o resto:
        vector::back
        vector::pop_back
    */

}